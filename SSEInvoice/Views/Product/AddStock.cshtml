
@*@{
    ViewData["Title"] = "AddStock";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model SSEInvoice.Models.Product

@{ var title = "Create Stock  Details"; }
<form method="post">
    <div class="row p-3 border">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="ProductId" />
        <input type="hidden" asp-for="Stocks.StocksId" />
        @if (Model == null || Model.ProductId != 0 || Model.Stocks.StocksId != 0)
        {
            //edit
            title = "Edit Stock Details";
        }
        <div class="col-12 border-bottom">
            <h2 class="text-primary">@title</h2>
        </div>
        <div class="col-8 pt-4">
            <div class="form-group row">
                <div class="col-4">
                    <label asp-for="ProductName"></label>
                </div>
                <div class="col-8">
                    <input asp-for="ProductName" disabled class="form-control" />
                </div>
            </div>
            <div class="form-group row">
                <div class="col-4">
                    <label asp-for="Stocks.Quantity"></label>
                </div>
                <div class="col-8">
                    <input asp-for="Stocks.Quantity" class="form-control" />
                    <span asp-validation-for="Stocks.Quantity" class="text-danger"></span>
                </div>
            </div>
            <div class="form-group row">
                <div class="col-4 offset-4">
                    @if (Model == null || Model.ProductId != 0 || Model.Stocks.StocksId != 0)
                    {
                        <button type="submit" class="btn btn-primary form-control">Update</button> }
                    else
                    {
                        <button type="submit" class="btn btn-primary form-control">Create</button>}
                </div>
                <div class="col-4">
                    <a asp-action="Index" class="btn btn-success form-control">Back to List</a>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts{
    <partial name="_ValidationScriptsPartial" />

}*@
